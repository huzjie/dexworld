apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dexworld
spec:
  selector:
    app: {{ .Release.Name }}-dexworld
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
